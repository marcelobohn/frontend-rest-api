# frontend-rest-api
Install node modules
npm install

start database server:
npm run serve

compile and run local:
gulp

structure:
/srv -> origin source 
/build -> distribution
/data -> json database 